# Backend environment example
# Copy to .env and adjust values.

# Primary client origin (production frontend URL) WITHOUT trailing slash
CLIENT_ORIGIN=https://leaderboard-system-1-1j3g.onrender.com
# Optional extra allowed origins (comma separated, no trailing slashes)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Set true ONLY for local debugging to temporarily allow any origin
CORS_ALLOW_ALL=false

# Mongo connection (Atlas SRV or standard). Do not include a port in mongodb+srv URI.
MONGO_URI=mongodb+srv://user:password@cluster.example.mongodb.net/leaderboard

# Server port (Render provides PORT automatically in production)
PORT=4000
